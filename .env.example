# =========================
# App
# =========================
PORT=10000
SESSION_SECRET=algo_super_secreto
NODE_ENV=production
TZ=America/Costa_Rica

# =========================
# OpenAI
# =========================
# Coloca tu clave REAL aquí solo en Render (Environment → Add Secret)
# NUNCA la subas a GitHub.
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Modelos válidos: por lo que pediste, usa GPT-5 Thinking
OPENAI_MODEL=gpt-5

# =========================
# MySQL (Railway - EXTERNO)
# =========================
DB_HOST=switchyard.proxy.rlwy.net
DB_PORT=24607
DB_USER=root
DB_PASSWORD=WzJMNfoPrnBRwpONpsQSUTjbWbTpfSnV
DB_NAME=railway
# Railway externo requiere SSL
DB_SSL=true

# (No uses el host interno de Railway en Render)
FORCE_RAILWAY_INTERNAL=0

# Variables estilo MYSQL* (opcional; algunas libs las leen):
MYSQLHOST=${DB_HOST}
MYSQLPORT=${DB_PORT}
MYSQLUSER=${DB_USER}
MYSQLPASSWORD=${DB_PASSWORD}
MYSQLDATABASE=${DB_NAME}

# =========================
# MySQL local (solo si pruebas en tu PC)
# =========================
LOCAL_DB_HOST=127.0.0.1
LOCAL_DB_PORT=3306
LOCAL_DB_USER=root
LOCAL_DB_PASS=
LOCAL_DB_NAME=flota

# =========================
# SMTP (corrige el HOST: no es un correo, es el servidor SMTP)
# Si usas Office365:
#   SMTP_HOST=smtp.office365.com
#   SMTP_PORT=587
#   SMTP_SECURE=false
#   SMTP_USER=notificaciones@tomza.com
#   SMTP_PASS=tu_password
# =========================
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=notificaciones@tomza.com
SMTP_PASS=CostaRica%Pass202
SMTP_FROM="Mantenimientos <notificaciones@tomza.com>"
SMTP_REPLY_TO=soporte@tomza.com
